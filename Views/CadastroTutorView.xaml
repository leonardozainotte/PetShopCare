<Window x:Class="PetShopCare.Views.CadastroTutorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Cadastrar Novo Tutor" Width="500" 
        Icon="pack://application:,,,/Images/logo.png"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">

    <Grid Margin="30,30,30,20">
        <StackPanel>
            <TextBlock Text="Cadastrar Novo Tutor" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,30"/>

            <TextBox materialDesign:HintAssist.Hint="Nome Completo *" Text="{Binding NovoCliente.Nome, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>
            <TextBox materialDesign:HintAssist.Hint="CPF *" Text="{Binding NovoCliente.CPF, UpdateSourceTrigger=PropertyChanged}" TextChanged="CpfTextBox_TextChanged" Margin="0,0,0,15"/>
            <TextBox materialDesign:HintAssist.Hint="Telefone *" Text="{Binding NovoCliente.Telefone, UpdateSourceTrigger=PropertyChanged}" TextChanged="TelefoneTextBox_TextChanged" Margin="0,0,0,15"/>
            <TextBox materialDesign:HintAssist.Hint="Endereco (Opcional)" Text="{Binding NovoCliente.Endereco, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

            <TextBlock Text="{Binding MensagemErro}" Foreground="Red" HorizontalAlignment="Center" Margin="0,10"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">

                <Button Content="CANCELAR" Command="{Binding CancelarCommand}" 
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Foreground="#7F8C8D" Margin="0,0,10,0" FontWeight="SemiBold"/>

                <Button Content="SALVAR" Command="{Binding SalvarCommand}" 
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="#1ABC9C" BorderBrush="#1ABC9C" Foreground="White" FontWeight="SemiBold" Padding="25,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>